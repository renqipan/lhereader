# lhereader
#############################################
lhereader.py defines most fuctions which are used to analyze lhe file
example1.py and example2.py are two basic examples for using the code lhereader.py.
hist_write2.py and hist_write1.py write lhe file into root file in histogram style.
tree_write.py write lhe file into root file in TBranch format
plot_var.c plots histograms generated by code hist_write2.py




##########################################
first example
###########################################
A Python module to read LHE (Les Houches Event) file and access the event information in an object oriented way.
Requires ROOT (root.cern.ch) and pyroot as external dependencies.

Sample code to use this module -

    from lhereader import readLHEF  
    from ROOT import TCanvas, TH1F  
    
    data=readLHEF('unweighted_events.lhe')  
    parts=data.getParticlesByIDs([5,-5]) # collect all botom and anti-bottom quarks  
    c=TCanvas()  
    hist=TH1F("pt", "Pt of b/#bar{b}",100,0,1000)  
    for p in parts:  
      hist.Fill(p.pt)  
    hist.Draw()  
    c.SaveAs("pt_b_bbar.png")  
